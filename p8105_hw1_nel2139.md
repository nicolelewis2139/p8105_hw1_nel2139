p8105_hw1_nel2139
================
Nicole Lewis
2026-09-23

# Section 0: Libraries

This code loads the tidyverse package.

``` r
library(tidyverse)
```

# Problem 1

## Loading the data

This code loads the `penguins` dataset from the `palmerpenguins`
package.

``` r
data("penguins", package = "palmerpenguins")
```

## Description of the dataset

The `penguins` dataset contains size measurements for adult foraging
penguins observed near Palmer Station, Antarctica, including each
penguin’s species, island in the Palmer Archipelago, size (flipper
length, body mass, and bill dimensions), and sex. The dataset has 344
rows and 8 columns and includes the following variables: `species`,
`island`, `bill_length_mm`, `bill_depth_mm`, `flipper_length_mm`,
`body_mass_g`, `sex`, and `year`.

The dataset covers three species (Adelie, Chinstrap, Gentoo) living on
three islands (Biscoe, Dream, Torgersen), observed from 2007 to 2009.
Bill length ranges from 32.1 to 59.6 mm and bill depth ranges from 13.1
to 21.5 mm. Flipper length ranges from 172 to 231 mm, with a mean value
of 200.9 mm. Body mass ranges from 2700 to 6300 g. Sex is recorded as
female or male, and is missing for 11 penguins.

## Plots

This code creates a scatterplot of flipper length (y) versus bill length
(x), with points colored by species. The second line of code exports the
scatterplot to the project directory.

``` r
ggplot(penguins, aes(x = bill_length_mm, y = flipper_length_mm, color = species)) + geom_point()
```

    ## Warning: Removed 2 rows containing missing values or values outside the scale range
    ## (`geom_point()`).

![](p8105_hw1_nel2139_files/figure-gfm/flip_vs_bill_plot-1.png)<!-- -->

``` r
ggsave("flip_vs_bill_plot.png")
```

    ## Saving 7 x 5 in image

    ## Warning: Removed 2 rows containing missing values or values outside the scale range
    ## (`geom_point()`).

# Problem 2

The chunk below sets a seed so the same random sample is generated each
time to ensure reproducibility and then creates a data frame containing
a sample of size 10 from a standard normal distribution constructs the
specified logical vector, creates a character vector of length 10, and
creates a factor vector of length 10 with three levels.

``` r
library(tidyverse)

set.seed(1)

prob2_df = tibble(
  norm_samp = rnorm(10, mean = 0, sd = 1),
  norm_samp_pos = norm_samp > 0, 
  vec_char = c("a", "b", "c", "d", "e", "f", "g", "h", "i", "j"),
  vec_factor = factor(c("low", "low", "low", "med", "med", "med", "med", "high", "high", "high"))
)
```
